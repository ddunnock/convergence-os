import { Meta } from "@storybook/addon-docs/blocks";
import { Introduction } from "./helpers/Introduction";

<Meta title="Introduction" />

<div style={{ marginBottom: "2rem", marginTop: "1rem" }}>
  <Introduction />
</div>

## Introduction

Convergence UI is a modern component library built with React, TypeScript, and Tailwind CSS. It features a unique glassmorphism design system with animated backgrounds, smooth transitions, and full dark mode support.

<br />

## Getting Started

Install the package:

```bash
pnpm add @convergence/ui
```

Include the providers in your `App.tsx`:

```tsx
import { ThemeProvider } from "@convergence/ui/providers";

function App() {
  return (
    <ThemeProvider defaultTheme="light">
      <YourApp />
    </ThemeProvider>
  );
}
```

<br />

## Features

- **Glassmorphism Design** - Modern glass-effect components with backdrop blur and translucency
- **Animated Backgrounds** - Floating orb animations with customizable colors
- **Dark Mode** - Full light and dark theme support with smooth transitions
- **Accessible** - Built with accessibility in mind using Radix UI primitives
- **TypeScript** - Full TypeScript support with exported types
- **Tailwind CSS v4** - Built on the latest Tailwind with CSS custom properties

<br />

## Color Palette

The design system uses a vibrant gradient palette:

| Color | Light Mode | Dark Mode | Usage |
|-------|------------|-----------|-------|
| Primary | `#7c3aed` | `#8b5cf6` | Primary actions, accents |
| Indigo | `#6366f1` | `#818cf8` | Background orbs, highlights |
| Purple | `#a855f7` | `#c084fc` | Gradient accents |
| Blue | `#3b82f6` | `#60a5fa` | Information, links |
| Pink | `#ec4899` | `#f472b6` | Destructive actions |

<br />

## Design Tokens

The library exposes CSS custom properties for easy customization:

```css
:root {
  /* Background gradients */
  --bg-gradient-start: #f0f4ff;
  --bg-gradient-mid: #e8eeff;
  --bg-gradient-end: #f2f6ff;

  /* Animated orb colors */
  --orb-color-1: #c7d2fe;
  --orb-color-2: #ddd6fe;
  --orb-color-3: #bfdbfe;
  --orb-opacity: 0.3;

  /* Glassmorphism */
  --glass: hsla(0, 0%, 100%, 0.7);
  --glass-border: hsla(240, 6%, 90%, 0.5);
}
```

<style>
  {`
  .sbdocs-content {
    max-width: 1000px;
  }

  table {
    width: 100%;
    border-collapse: collapse;
    margin: 1rem 0;
  }

  th, td {
    padding: 0.75rem;
    text-align: left;
    border-bottom: 1px solid var(--glass-border, rgba(200, 200, 220, 0.3));
  }

  th {
    font-weight: 600;
  }

  code {
    background: var(--glass, rgba(200, 200, 220, 0.2));
    padding: 0.125rem 0.375rem;
    border-radius: 0.25rem;
    font-size: 0.875rem;
  }

  pre {
    background: var(--glass, rgba(200, 200, 220, 0.2)) !important;
    border: 1px solid var(--glass-border, rgba(200, 200, 220, 0.3));
    border-radius: 0.5rem;
    padding: 1rem;
    overflow-x: auto;
  }

  pre code {
    background: transparent;
    padding: 0;
  }
  `}
</style>
